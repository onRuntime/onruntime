---
"@onruntime/translations": minor
---

Add ICU `plural` and `select` arguments to messages, with the plural category resolved from `Intl.PluralRules` in the locale being read so languages with more than two forms are correct
